

Arenas:
    when creating an arena, they should be created from a common memory allocator, and given a name
    when they allocate or free memory, they log their memory usage internally,
    the memory allocator/system keep a pointer reference to all of them, and when it wants, iterates over them and logs/prints their memory usage
    this will give me find grain controller over memory management





intrusive containers: (they store references to pointers, but do not themselves own the data)
    array
    darray
    hash map
    hash table
    linked list
    binary tree
    queue
    stack
